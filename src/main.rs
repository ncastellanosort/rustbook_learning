use std::process;

mod cap10_generic_lifetimes;
mod cap11_testing;
mod cap13_closures;
mod cap4_ownership;
mod cap5_structs;
mod cap6_enums;
mod cap8_colecciones;
mod cap9_result;
mod minigrep;

fn main() {
    /* minigrep
    if let Err(e) = minigrep::run(config) {
        eprintln!("error en aplicacion: {e}");
        process::exit(1);
    }
    */
}
